<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme: no action bar, full dark background -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="colorPrimary">@color/btn_active_border</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">@color/btn_active_text</item>
    </style>

    <!-- Shared base for all menu items (active or disabled) -->
    <style name="MenuItemBase">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">13sp</item>
        <item name="android:letterSpacing">0.10</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <!-- Active (clickable) button style -->
    <style name="MenuItemActive" parent="MenuItemBase">
        <item name="android:background">@drawable/btn_active_bg</item>
        <item name="android:textColor">@color/btn_active_text</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <!-- Disabled (greyed-out) button style -->
    <style name="MenuItemDisabled" parent="MenuItemBase">
        <item name="android:background">@drawable/btn_disabled_bg</item>
        <item name="android:textColor">@color/btn_disabled_text</item>
        <item name="android:clickable">false</item>
        <item name="android:focusable">false</item>
    </style>

</resources>
